funktion createHTMLList(list){
    let ergebnis = ""

    let htmlElemnts = liste.map((listtItem) => `<li>${listItem}</li>`)
    console.log(htmlElemnts)
    return ergebnis
}

my_list = ["Nova", "Toni"]
createHTMLList(my_list)